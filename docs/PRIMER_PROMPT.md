# Primer prompt de la sesión (referencia)

```
Lee el archivo @PRD.md. Actúa como un desarrollador Senior especializado en Flutter y Node.js. Vamos a construir el proyecto descrito en el documento estrictamente paso a paso. No generes el código de toda la aplicación ni adelantes módulos.

Para nuestra primera iteración, haz lo siguiente:
1. Proporciona los comandos de terminal exactos para inicializar el proyecto en Flutter.
2. Proporciona los comandos de terminal (en bash/zsh) para crear la estructura de carpetas inicial dentro de `lib/`, separando `core/` y `features/` según la arquitectura Feature-First detallada en el documento.

Detente ahí. Espera mi confirmación de que la estructura se creó correctamente antes de pasar a la configuración de Firebase Auth y el ruteo.
```

**Nota:** En el workspace el archivo se llamaba `PDR.md` (no `PRD.md`).
